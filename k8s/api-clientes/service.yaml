apiVersion: v1
kind: Service
metadata:
  name: api-clientes-svc
spec:
  type: NodePort
  selector:
    app: api-clientes
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30082 # A porta que você usará no localhost (ex: localhost:30082)