apiVersion: v1
kind: Service
metadata:
  name: api-clientes-svc
spec:
  type: NodePort
  selector:
    app: api-clientes
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30082